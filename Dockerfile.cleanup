FROM alpine:latest

RUN apk add --no-cache sqlite bash busybox-suid

COPY cleanup.sh /usr/local/bin/cleanup.sh
RUN chmod +x /usr/local/bin/cleanup.sh

# Create crontab entry
RUN echo "0 2 * * * /usr/local/bin/cleanup.sh" > /var/spool/cron/crontabs/root

CMD exec crond -f -l 2
